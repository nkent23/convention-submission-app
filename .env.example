# Neon Postgres connection string
DATABASE_URL=

# Secret for signing admin session cookies (generate: openssl rand -hex 32)
SESSION_SECRET=

# Public base URL of the deployed app
NEXT_PUBLIC_APP_URL=http://localhost:3000
